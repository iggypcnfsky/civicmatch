/**
 * Profile Components
 * 
 * Reusable components for profile display, gamification, and activity tracking
 */

// Import component styles
import './profile.css';

// Core components
export { ActivityTimeline, ActivityStats } from './ActivityTimeline';
export { UpcomingEvents } from './UpcomingEvents';

// Gamification components (available for future use)
export { LevelBadge, LevelIndicator } from './LevelBadge';
export { ExperienceMeter, XPBadge } from './ExperienceMeter';
export { BadgesDisplay, BadgeChip, BadgeProgress } from './BadgesDisplay';
